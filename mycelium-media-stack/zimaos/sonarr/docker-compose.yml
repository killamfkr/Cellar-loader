name: sonarr

services:
  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    restart: unless-stopped
    networks:
      - mycelium-media
    ports:
      - target: 8989
        published: "8989"
        protocol: tcp
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    volumes:
      - type: bind
        source: /DATA/AppData/mycelium-media-stack/sonarr
        target: /config

networks:
  mycelium-media:
    external: true

x-casaos:
  app_id: com.cellarloader.sonarr
  main: sonarr
  author: Cellar-loader
  developer: LinuxServer.io
  category: Media
  description:
    en_us: TV list manager. Connect to Mycelium Admin for bulk import (not a download client).
  tagline:
    en_us: Sonarr for Mycelium stack
  icon: https://cdn.jsdelivr.net/gh/IceWhaleTech/CasaOS-AppStore@main/Apps/Sonarr/icon.svg
  index: /
  scheme: http
  port_map: "8989"
  store_app_id: sonarr
  title:
    en_us: Sonarr
